//-           Options                  |               Classes
    -------------------------------------------------------------------------------
    state - (bool = false) - вкл/выкл  | element - элементом какого блока является
                                       | addAnyClass - добавление свободных классов
                                       | mode - модификатор




mixin toggle(options)
    if !options
        -var options = {}
    -
        classes += options.state ? ' toggle-on' : ''
    .toggle(class=pf.addClasses('toggle', options) class= classes)


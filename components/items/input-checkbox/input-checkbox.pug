//-           Options              |               Classes    ---------------------------------------------------------------------------    options.name - name            | options.element - класс элемента    options.description - описание | options.mode - модификатор блока    options.id - id                | options.state - модификатор состояния    options.info - подпись в label | options.addAnyClass - добавить любой класс                                   | options.class - class input-amixin input-checkbox(options)  if !options    -options= {}  if !options.id || !options.info    p Указание id и info являются обязательными для input-checkbox  -    var classes = 'input-checkbox'    classes += options.element ? ' ' + options.element : ''    classes += options.mode ? ' input-checkbox_' + options.mode : ''    classes += options.state ? ' input-checkbox_state_' + options.state : ''    classes += options.addAnyClass ? ' ' + options.addAnyClass : ''  div(class=classes)    input(      class="input-checkbox__element"      type="checkbox"      name= options.name      id= options.id    )    label.input-checkbox__signature(for=options.id)      span= options.info    if options.description      p.input-checkbox__description= options.description
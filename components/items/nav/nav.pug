//-           Options                       |               Classes
    ----------------------------------------------------------------------------
     listMenu - object                      | options.element - элементом какого блока я вляется
        (text, drop - стрелка, link - ссыль)| option.mode - модификатор данного блока
                                            | option.addAnyClass - свободное добавление классов к блоку
// TODO @Kotaro сделать автоматическое создание nav если не переданны аргументы
        @Misha: В адаптиве в хедаре всё плохо.
mixin nav(options)
    if !options
        - options = {}
    -
        var classes = 'nav'
        classes += options.mode ? ' nav_' + options.mode : ''
        classes += options.element ? ' ' + options.element + '__nav' : ''
        classes += options.addAnyClass ? ' ' + options.addAnyClass : ''
    div(class=classes)
        for text in options.listMenu
            if !text.link
                - text.link = '#'
            if text.drop
                a.nav__link.nav_drop(href=text.link)= text.text
                    .nav_arrow keyboard_arrow_down

            else
                a.nav__link(href=text.link)= text.text

+nav({
    listMenu: [
        {text: 'Главная', link: '#'},
        {text: 'О нас', link: 'https://google.com'},
        {text: 'Главная', link: '#', drop: true},
        {text: 'Главная', link: '#'},
        {text: 'Главная', link: '#'}
    ]
})
//-           Options                |               Classes                       |   Includes
    -------------------------------------------------------------------------------|-------------------
    options.image - путь до картинки | options.element - класс элемента            | like-button
    options.author - имя автора      | options.mode - модификатор блока            |  count - кол-во лайков
    options.data - дата публикаци    | options.state - модификатор состояния       |  state (bool = false) - лайкнуто или нет
    options.text - текст отзыва      | options.addAnyClass - добавить любой класс  |
                                     | options.class - class input-a               |
include ../../items/like-button/like-button
// TODO: @misha Почему-то текст не ровный. Скорее всего, из-за того, что на мегу когда-то залилось плохо или забыли обнавить. + like-button здесь уменьшается в размерах.
    .simple-text Стили, которые были подключены у меня, но в общем проекте их уже нет. 
    js файл пустой.
mixin feedback-record(options)
    .feedback-record
        .feedback-record__top
            script
                import '#{options.image}'
            img(src=options.image, alt=options.author)
            .feedback-record__title
                .feedback-record_author= options.author
                .simple-text= options.data
        .feedback-record__bottom
            +like-button({
                count: options.count,
                state: options.state,
                element: 'feedback-record'
            })
            .simple-text= options.text



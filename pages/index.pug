<meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">

include ../components/items/toggle/toggle
+toggle({})



mixin block(options)
    if !options
        - options = pf.helper({keks: true})
    div(class= pf.addClasses('block', options)) Блок с классами


+block()